describe('Database', () => {

  beforeAll(async (done) => {
    done();
  });

  // afterAll(() => server.close());

  it('should be running on url from environment', async (done) => {
    done();
  });
});
